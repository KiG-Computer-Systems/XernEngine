// Менеджер ресурсов
export class ResourceManager {
    constructor() {
        this.images = {};
        this.sounds = {};
    }

    loadImage(name, url) {
        // ...логика загрузки изображения...
    }

    loadSound(name, url) {
        // ...логика загрузки звука...
    }
}
